# Website Drop Folder

Drop each website into its own folder here, with an `index.html` at the top of that folder.

Example:

```text
Website/
  Project Name/
    index.html
    assets/
```

The portfolio's Website section will embed each discovered `index.html`. Use relative asset paths inside the dropped website when possible.
